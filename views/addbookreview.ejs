<!DOCTYPE html>
<html>
<head>
    <title><%= forumName %> - Add Review</title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
</head>
<body>
    <%- include('./partials/header.ejs'); -%>

    <div class="container">
        <h2>Add Review for <%= book.title %></h2>
        <div class="review-flex-container">
            <% if (book.thumbnail) { %>
                <div class="book-thumbnail">
                    <img src="<%= book.thumbnail %>" alt="<%= book.title %>">
                </div>
            <% } %>
            <div class="card review-card">
                <form method="POST" action="/submitreview">
                    <div class="form-group">
                        <label for="postTitle">Review Text:</label>
                        <textarea name="postTitle" id="postTitle" required></textarea>
                    </div>
                    <div class="form-group">
                        <label for="reviewText">Review Text:</label>
                        <textarea name="reviewText" id="reviewText" required></textarea>
                    </div>
                    <div class="form-group">
                        <label for="rating">Rating:</label>
                        <input type="number" name="rating" id="rating" min="1" max="5" required />
                    </div>
                    <input type="hidden" name="bookId" value="<%= book.id %>">
                    <input type="hidden" name="thumbnail" value="<%= book.thumbnail %>">
                    <button type="submit" class="btn">Submit Review</button>
                </form>
            </div>
        </div>

        <% if (errorMessage) { %>
            <div class="error-message"><%= errorMessage %></div>
        <% } %>
    </div>
</body>
</html>
